name: Marmara Games Pure Cloud Build

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # 1. Adım: Depodaki tüm dosyaları bulut sunucusuna kopyalıyoruz
    - name: Checkout Source Code
      uses: actions/checkout@v4

    # 2. Adım: Derleme için gerekli olan Java (JDK 17) motorunu kuruyoruz
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: 'gradle'

    # 3. Adım: Proje kök dizinini otomatik bulup Gradle motorunu ateşliyoruz
    - name: Find Gradle Root and Build Release APK
      run: |
        GRADLE_ROOT=$(find . -name "settings.gradle" -exec dirname {} \; | head -n 1)
        
        if [ -z "$GRADLE_ROOT" ]; then
          echo "HATA: settings.gradle dosyası depoda hiçbir yerde bulunamadı!"
          exit 1
        fi
        
        echo "Gradle proje dizini bulundu: $GRADLE_ROOT"
        cd $GRADLE_ROOT
        
        # Çalıştırma iznini kontrol edip tetikliyoruz
        if [ -f ./gradlew ]; then
          chmod +x gradlew
          ./gradlew assembleRelease
        else
          gradle assembleRelease
        fi

    # 4. Adım: Sadece oluşan .apk dosyasını indirme linki olarak çıkartıyoruz
    - name: Upload Marmara Games APK
      uses: actions/upload-artifact@v4
      with:
        name: Marmara-Games-Professional-APK
        path: |
          **/build/outputs/apk/release/*.apk
        
